pipeline {
     agent any
   stages {
       stage ('compile stage') {
	    steps {
		   sh '/usr/local/maven/bin/mvn clean'
		}
	}
	  stage ('testing stage') {
	   steps {
		       sh '/usr/local/maven/bin/mvn test'
		   }
	   }
	  stage ('deployment') {
	   steps {
	           sh '/usr/local/maven/bin/mvn deploy'
	   }
	   }
	  }
	}
